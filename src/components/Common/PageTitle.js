import styled from "styled-components"

const PageTitle = styled.h1`
  font-family: Vollkorn SC;
  font-style: normal;
  font-weight: normal;
  font-size: 40px;
  line-height: 56px;
  color: #000000;
`
export { PageTitle }
